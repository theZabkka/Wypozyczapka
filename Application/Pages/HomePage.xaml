<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Application.Pages.HomePage"
             BackgroundColor="#F8F8FF"
             Shell.NavBarIsVisible="False"
             Shell.TabBarIsVisible="False">

    <Grid>

        <Image Source="tlo.png"
               Aspect="AspectFill"
               Opacity="0.2"
               ZIndex="0" />

        <!-- Pasek tytułowy z hamburgerem i tytułem -->
        <Border BackgroundColor="White"
                HeightRequest="60"
                VerticalOptions="Start"
                HorizontalOptions="Fill"
                ZIndex="1">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="0,0,0,0" />
            </Border.StrokeShape>

            <Grid>

                <!-- Hamburger -->
                <ImageButton Source="flyoutmenuicon.png"
                             WidthRequest="32"
                             HeightRequest="32"
                             BackgroundColor="Transparent"
                             Clicked="OnMenuClicked"
                             HorizontalOptions="Start"
                             VerticalOptions="Center"
                             ZIndex="1"
                             Margin="5,0"/>

                <!-- Tytuł -->
                <Label Text="WypożyczApka"
                       HorizontalOptions="Center"
                       VerticalOptions="Center"
                       FontSize="28"
                       FontAttributes="Bold"
                       TextColor="Black"
                       ZIndex="2"/>
            </Grid>
        </Border>

        <VerticalStackLayout Margin="20,90,20,0" Spacing="15" ZIndex="3">

            <!-- Baner: Wypożyczaj -->
            <Border BackgroundColor="#FFEFD5"
            StrokeThickness="0"
            HeightRequest="70"
            Padding="15"
            StrokeShape="RoundRectangle 20"
            ZIndex="3">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnWypozyczTapped"/>
                </Border.GestureRecognizers>
                <Grid ColumnDefinitions="Auto,*"
              VerticalOptions="Center">
                    <Image Source="wypozycz.png"
                   WidthRequest="30"
                   HeightRequest="30"
                   VerticalOptions="Center"/>
                    <Label Text="Wypożyczaj"
                   FontSize="20"
                   FontAttributes="Bold"
                   TextColor="#4A4A4A"
                   VerticalOptions="Center"
                   Margin="15,0,0,0"
                   Grid.Column="1"/>
                </Grid>
            </Border>

            <!-- Baner: Aktualności -->
            <Border BackgroundColor="#E0FFFF"
            StrokeThickness="0"
            HeightRequest="60"
            Padding="15"
            StrokeShape="RoundRectangle 20"
            ZIndex="3">
                <Grid ColumnDefinitions="Auto,*"
              VerticalOptions="Center">
                    <Image Source="news.png"
                   WidthRequest="25"
                   HeightRequest="25"
                   VerticalOptions="Center"/>
                    <Label Text="Aktualności"
                   FontSize="18"
                   FontAttributes="Bold"
                   TextColor="#2F4F4F"
                   VerticalOptions="Center"
                   Margin="10,0,0,0"
                   Grid.Column="1"/>
                </Grid>
            </Border>

        </VerticalStackLayout>

        <!-- Lista kafelków z aktualnościami -->
        <CollectionView ItemsLayout="VerticalList"
                        Margin="20,230,20,20"
                        x:Name="NewsCollectionView"
                        ZIndex="2">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame CornerRadius="20"
                           HasShadow="True"
                           Margin="0,10"
                           Padding="0"
                           BackgroundColor="White">
                        <Grid RowDefinitions="Auto,Auto"
                              ColumnDefinitions="Auto,*">
                            <Image Source="{Binding ImageSource}"
                                   WidthRequest="100"
                                   HeightRequest="100"
                                   Margin="10"
                                   Aspect="AspectFill"
                                   Grid.RowSpan="2"/>
                            <Label Text="{Binding Title}"
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="#333"
                                   Margin="10,10,10,0"
                                   Grid.Column="1"/>
                            <Label Text="{Binding Description}"
                                   FontSize="14"
                                   TextColor="#555"
                                   Margin="10,0,10,10"
                                   Grid.Column="1"
                                   Grid.Row="1"/>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>
</ContentPage>
